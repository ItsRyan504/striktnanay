<#
  Deprecated: device_apps workaround
  This project no longer uses the device_apps plugin.
  UsageStats (via native MethodChannel) replaces all device/installed app listing needs.
  This script is intentionally left as a no-op for historical reference.
#>
Write-Host "device_apps workaround is deprecated and not used." -ForegroundColor Yellow

